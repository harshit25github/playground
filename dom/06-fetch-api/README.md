# DOM 06 - Fetch API (talk to your backend)

1. Start the backend:
   - `playground/10-node-http-api/` → `node server.js`
2. Open `index.html`

Goal: fetch todos, render them, and create a new todo using `fetch()`.

