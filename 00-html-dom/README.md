# 00 - HTML + DOM basics

Open `index.html` in your browser and complete the TODOs in `app.js`.

## What you practice

- `querySelector`, `addEventListener`
- reading/writing text + attributes
- rendering a list from an array
- localStorage basics

